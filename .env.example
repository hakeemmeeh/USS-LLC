# Public site URL for canonical URLs, Open Graph, and sitemap (no trailing slash).
# Copy to .env.local and set for local preview if needed.
NEXT_PUBLIC_SITE_URL=https://unitedsocialservices.com

# Optional: wire the contact API to email (see src/app/api/contact/route.ts).
# RESEND_API_KEY=
# CONTACT_INBOX=
